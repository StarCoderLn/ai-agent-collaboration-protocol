-- 平台内置设计与代码 Agent 的最低报价必须落在当前冷启动单笔上限内。否则所有同类
-- Agent 都会因前三单无法成交而永久停留在观察期。只修改代码库拥有的两个稳定 Agent；
-- 用户 Agent、历史候选和已冻结报价均保持不变。
BEGIN;

UPDATE agents
   SET price_amount = 1500000,
       updated_at = now()
 WHERE id IN (
   '91000000-0000-4000-8000-000000000004',
   '91000000-0000-4000-8000-000000000007'
 );

COMMIT;
