# 项目工程协作规则

本文件适用于整个仓库，是 Codex、Claude 及其他开发 Agent 必须遵循的项目级指令。三本书的完整思想提炼见 [工程方法论](docs/engineering-philosophy.md)；本文件只保留会改变实际行为的规则。

若规则冲突，依次遵循：用户当前请求 > 更具体目录的规则 > 本文件。无法安全判断时，必须说明冲突并请求确认，不得静默选择。

**工作流保护：** 第 1–11 节是稳定规则区。`yd-ai-wf-opt` 或其他自动化只能压缩、去重和下沉第 12 节的经验条目，不得改写、搬迁或删除稳定规则区；确需修改时必须由用户明确授权。

## 1. 核心模型

- **《无穷的开始》：** 先提出受证据约束、难以随意改变的解释，再通过批评、反例和实验纠错；不得用权威、惯例或新增特例代替根因分析。
- **《软件设计的哲学》：** 以降低修改扩散、认知负担和未知依赖为目标；优先深模块、简单接口、信息隐藏和单一权威知识源。
- **《重构（TypeScript 版）》：** 在行为基线保护下小步调整结构；重构与功能变更必须可区分，由真实坏味道驱动，不为审美或假想需求大改代码。

统一工作循环：**发现问题 → 提出解释 → 明确不变量 → 设计边界 → 小步实现 → 验证反例和失败路径 → 沉淀知识。**

## 2. 强制执行门禁

下面的门禁采用“触发条件 → 必须动作 → 验收证据”格式。满足触发条件时不得跳过。

| 编号 | 触发条件 | 必须动作 | 验收证据 |
| --- | --- | --- | --- |
| G1 | 任何任务开始 | 明确目标、约束、不变量、非目标和完成标准；检查适用规则与工作区状态 | 开始实施前有简洁的问题定义，且未覆盖用户已有修改 |
| G2 | 行为不明确、缺陷诊断或存在多个解释 | 区分事实、推断和未知；检查反例、失败路径与最小复现 | 根因由代码、测试、日志或可重复实验支持 |
| G3 | 修改公共 API、协议、数据库、认证、资金、并发、部署或跨系统流程 | 实施前提供高影响决策摘要，比较真实候选方案及恢复方式 | 用户可审查目标、不变量、范围、未知、方案权衡与验证计划 |
| G4 | 新增模块、抽象、依赖或共享状态 | 说明它隐藏了什么复杂度以及为何现有边界无法承担 | 新接口比内部实现简单；不存在只转发调用的浅封装 |
| G5 | 重构已有代码 | 先建立测试、特征测试或可观察行为基线；将重构与行为变化分阶段 | 每个安全检查点的相关验证通过，外部行为保持不变 |
| G6 | 修复缺陷 | 先用测试或最小复现捕获缺陷，再修复根因和同类可达入口 | 回归测试修复前失败、修复后通过，或有等价可复验证据 |
| G7 | 任务完成 | 运行与风险相称的测试、类型检查、lint、构建及必要人工检查 | 最终报告实际修改、已验证结果、未验证项和剩余风险 |

如果缺少执行门禁所需的信息，先进行安全、只读、可逆的检查。只有信息无法获得且不同选择会实质改变结果时，才向用户提问。

## 3. 高影响任务

满足任一条件即按高影响任务处理：

- 修改公共 API、外部协议、数据库 schema、migration 或持久化格式
- 涉及认证、授权、隐私、钱包、金额、托管、结算、退款或争议
- 涉及部署、基础设施、生产数据、不可逆操作或第三方系统写入
- 引入新依赖、共享状态、并发机制，或跨越多个模块和服务边界
- 改变幂等、事务、重试、超时、错误处理、补偿或状态恢复语义
- 现有行为缺少验证基线，或错误设计会造成明显返工和业务风险

实施前必须给出以下决策摘要：

1. **触发条件：** 为什么属于高影响任务。
2. **目标与不变量：** 哪些行为和数据必须保持正确。
3. **范围与非目标：** 允许修改什么，明确不做什么。
4. **关键未知：** 如何用代码、文档、实验或真实环境验证。
5. **候选方案：** 仅在存在真实选择时比较接口复杂度、信息泄漏、修改扩散、可逆性、测试和恢复。
6. **验证计划：** 正常、边界、失败、恢复和回归路径如何证明。

## 4. 问题理解与好解释

1. 必须区分：已验证事实、基于证据的推断、尚未确认的未知。
2. 解释必须能说明因果机制，并能排除主要替代解释；“框架就是这样”“以前这么做”“最佳实践”不构成证据。
3. 如果方案需要不断增加例外、布尔开关或局部补丁，应停止并重新检查领域模型和模块边界。
4. 不确定时优先做最小、可逆、能产生信息的实验，不用大规模修改验证未经约束的假设。
5. 发现文档、代码和实际行为不一致时，明确指出差异，以实际验证为准，不静默选择其中一个。
6. 批评针对方案；面对用户异议时提供具体证据和权衡，不以无依据的顺从或权威结束讨论。

## 5. 设计与复杂度

### 5.1 必须遵循

1. 优先设计深模块：接口简单、稳定、完整，复杂决策由模块内部吸收。
2. 同一业务规则、状态含义、格式或协议知识只保留一个权威实现。
3. 隐藏存储、第三方 SDK、重试策略和易变决策，禁止要求所有调用方理解内部细节。
4. 正确用法应自然，错误用法应困难；参数、返回值、状态、副作用和失败语义必须明确。
5. 关键接口至少形成两个真正不同的草案，再比较接口深度、信息泄漏、修改扩散和测试难度。
6. 如果模块内部多做少量工作能显著简化多数调用方，应把复杂度向下吸收。
7. 注释解释契约、意图、不变量和反直觉原因，不复述代码。

### 5.2 禁止做法

- 只转发调用或改名、没有隐藏复杂度的浅封装
- 同一业务判断散落在控制器、服务、任务和 UI 多处
- 为未经确认的未来需求创建框架、插件点和配置开关
- 用全局共享状态、隐式初始化顺序或消息链隐藏依赖
- 用捕获并忽略、返回空值或模糊错误码假装“消除异常”
- 为局部代码简短牺牲系统整体接口和失败恢复的清晰度

## 6. 实现与重构

1. 修改保持在完成目标所需的最小合理范围；覆盖已确认根因和同类入口，但不“顺便优化”无关内容。
2. 重构保持外部可观察行为不变。无法证明不变的修改按行为变更处理。
3. 先重构、验证，再增加功能；不得在同一步混合改名、搬迁、抽象重组和业务变化。
4. 采用小步修改，每个安全检查点运行最快、最相关的验证，确认后再继续。
5. 坏味道只是调查信号。先判断根因，再选择提炼函数、移动职责、引入参数对象、拆分阶段、封装变量或显式状态建模。
6. 性能优化必须有测量证据；先保持结构清晰，再优化已确认的热点。
7. 达到验收标准后停止。其他改进记录为后续建议，不扩大当前任务。
8. 项目自有源码的说明性注释必须使用中文，并详细解释契约、意图、不变量和反直觉原因；
   许可证标识、自动生成文件、已发布迁移文件、协议字段、代码签名与文件路径可保留必要
   英文技术标识，禁止仅为翻译注释改写不可变迁移历史。

重点调查以下坏味道：重复规则、过长函数、职责模糊、数据泥团、基本类型偏执、过长参数、特性依恋、中间人、调用链、大量嵌套条件、布尔参数、死代码、霰弹式修改、发散式变化和投机式通用化。

## 7. TypeScript 规则

1. 启用并保持 `strict`；不得用 `any`、非空断言或宽泛类型掩盖未知。
2. 外部输入必须在边界做运行时校验，再转换为内部可信类型。
3. 互斥状态使用可辨识联合；任务与资金流程使用显式状态机，禁止非法组合可表示。
4. 为任务 ID、Agent ID、钱包地址、金额、币种、时间等建立领域类型，避免全部退化为 `string`/`number`。
5. 金额使用最小单位整数或可靠的 decimal/bigint 方案，禁止浮点数处理资金。
6. 纯领域计算与数据库、网络、时间、随机数等副作用分离。
7. 类型断言只允许出现在已验证边界附近，并必须能解释为何安全。
8. 不为测试破坏封装；测试公开行为和领域不变量，通过依赖注入隔离外部环境。

## 8. 测试与验证

- **特征测试：** 行为不明确时记录现状，只用于防止无意改变，不代表现状正确。
- **单元测试：** 覆盖领域规则、金额计算、状态迁移和错误定义。
- **契约测试：** 固定平台与 Agent、钱包、链服务和回调之间的请求、响应、认证及错误语义。
- **集成测试：** 覆盖数据库事务、队列、幂等键、重试、超时和适配器协作。
- **端到端测试：** 覆盖最关键闭环与高风险恢复路径，不用大量脆弱 UI 测试替代低层验证。

测试应断言行为和不变量，不耦合内部调用顺序。测试失败时先判断是行为变化、测试错误还是既有缺陷，禁止为了变绿削弱断言。

不得声称未执行的验证已通过。真实部署、第三方配置、链上结果或人工审批未完成时，必须明确说明仍待外部验证。

## 9. 项目关键不变量

- 认证、授权、任务归属和数据可见性在可信边界统一校验。
- 任务、托管、交付、验收、争议和结算状态迁移必须显式、合法、可审计。
- 外部 Agent、链服务和回调默认不可靠；必须考虑认证、超时、幂等、重放防护、重试上限、补偿和人工恢复。
- 金额、币种、精度、费率、舍入和结算规则必须集中实现并全链路一致。
- 高风险操作明确展示对象、金额、状态、后果和不可逆性，并保留审计证据。
- 日志不得记录密钥、完整凭据、敏感任务内容或不必要的个人信息。
- Agent 输出属于不可信输入，必须校验、隔离并限制其可触发的工具与数据权限。

## 10. 权限与交付

1. 诊断、解释、审查和状态查询默认只读；除非用户要求修复或实现，不得擅自修改。
2. 修改任务应推进到可验证结果，但提交、推送、发布、部署和不可逆外部操作需要用户明确授权。
3. 尊重用户已有和未提交的修改，不覆盖、回退或整理无关内容。
4. 优先使用项目已有命令、测试和工具链，不建立平行流程。
5. 最终交付必须报告：实际修改、执行的验证及结果、未执行的验证及原因、剩余风险和必要后续动作。

## 11. 完成前检查

- 根因是否有证据，是否检查了反例和失败路径？
- 方案是否减少修改扩散、认知负担和未知依赖？
- 接口是否比实现简单，业务知识是否集中在权威位置？
- 是否引入浅封装、投机抽象、重复规则或隐藏依赖？
- 重构与行为变化是否分离，并有行为基线保护？
- 正常、边界、失败、恢复和相关回归是否完成相称验证？
- 资金、安全、隐私、外部 Agent 和不可逆操作是否有显式保护？
- 最终结论是否严格对应实际 diff、测试结果和外部状态？

## 12. 工作流经验沉淀区

本节供 `yd-ai-wf-opt` 等工作流追加可复用教训。每条必须满足：

- 使用 `- [feature/taskId] 结论；以后如何规避` 格式且不超过 80 字。
- 只记录未来可能再次遇到、能改变后续决策或操作的项目教训。
- 一次性业务细节、修复过程、代码片段和已被稳定规则覆盖的内容不得写入。
- 模块专属教训应下沉到对应模块目录的 `AGENTS.md`；这里只保留跨模块硬约束。
- 允许去重、合并和压缩本节条目，但不得丢失仍然有效的知识。

### 已沉淀条目

- [1.agent-protocol-contract/T-002] 已记录的历史教训需在下个 task 验证是否真落地,不能只记不查。
- [2.agent-registration/T-004,T-006,T-008] 勾选 task 完成前须核对：未独立验证的其它 task 不可批量勾选、声明的依赖须已勾选、tasks.md 内『未完成原因』等说明须同步更新/删除，避免自相矛盾。
- [2.agent-registration/T-007] task 完成判定以其自身声明范围内的证据(如测试通过)为准，不应因未被列为其依赖的其他缺口而无限期不勾选，但须在文档保留该已知限制说明。
- [product-workflow] 新 Agent 通过异常输出测试和虚构任务真实模型冒烟后才能标记可用。
- [product-workflow] 下游须消费已验收上游制品并验证继承；禁止只传摘要后重做。
- [product-workflow] Coding 须继承设计稿视觉与结构；禁止用平台固定 CSS 覆盖。
- [product-workflow] 返工须携带可重建完整上下文；禁止依赖 Agent 进程内首次派发缓存。
- [escrow] 仅无 Deposit 哈希和链事件的明确失败可改选；未确认不等于未广播。
- [9.dispatch-and-acceptance] 托管意图创建即锁定选人与总价；此前改选须事务重算。
- [6.escrow-sync-and-wallet] 本地 Anvil 交易须读 pending nonce，避免钱包跨实例缓存造成队列空洞。
- [13.dispute-and-arbitration] 本地 TestYD 不得覆盖钱包目录中的 Sepolia 产品 YD。
- [3.agent-health-lifecycle] 新标识、冷启动资金风控和评分置信度须使用各自事实，禁止复用阈值。
- [18.stagehand-browser-agent] Sepolia 演示须用统一启动器恢复服务，禁止遗漏 9202。
