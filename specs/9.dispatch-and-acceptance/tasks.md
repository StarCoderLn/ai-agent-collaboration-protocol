# 派发与接单确认 — 任务清单

## 任务版本

| 日期       | 版本 | 说明     |
| ---------- | ---- | -------- |
| 2026-08-20 | v1   | 初始任务 |

## 项目信息

- 项目名: ai-agent-collaboration-protocol
- 架构类型: 多服务架构
- specs 路径: specs/9.dispatch-and-acceptance/

## 任务列表

### 功能 1: 数据模型与原子占用

- [ ] T-001: 编写 `task_assignments`（唯一部分索引）、`dispatch_attempts` 表 migration ~30min
- [ ] T-002: 实现 `LockAssignment()`，接入候选确认 API 与自动分配路径 ~30min

### 功能 2: SQS 派发

- [ ] T-003: 实现 SQS 派发生产者（幂等键、签名调用 Agent） ~30min
- [ ] T-004: 实现 Agent 接单/拒单回调处理（签名验证、状态更新） ~30min

### 功能 3: 超时处理

- [ ] T-005: 实现接单超时扫描任务（标记接单失败、任务回待匹配） ~30min

### 功能 4: 前端

- [ ] T-006: 实现候选确认操作与分配状态展示（含被拒/超时后重新选择候选） ~30min

### 集成与测试

- [ ] T-007: 编写测试：并发确认只有一个成功、幂等派发不重复分配、超时未接单流转 ~30min

## 依赖关系

- T-002 依赖 T-001
- T-003、T-004 依赖 T-002
- T-005 依赖 T-004
- T-006 依赖 T-002、T-005
- T-007 依赖 T-002、T-003、T-004、T-005
- 跨 feature 依赖：T-003、T-004 依赖 `1.T-002`、`1.T-003`、`1.T-005`（签名与幂等中间件）；T-002 依赖 `8.T-005`（候选集合已生成）

## 风险点

- 接单确认时限为占位默认值（见 requirements.md 开放问题），需上线前依据真实 Agent 响应时间分布复核。
