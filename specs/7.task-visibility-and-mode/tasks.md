# 任务可见性与分配模式 — 任务清单

## 任务版本

| 日期       | 版本 | 说明     |
| ---------- | ---- | -------- |
| 2026-08-20 | v1   | 初始任务 |
| 2026-08-20 | v2   | T-004 增加受控上线期预算上限判断（调用 [[3.agent-health-lifecycle]] 的 `IsInProbation()`）；T-008 增补对应测试；任务结构和数量不变 |
| 2026-08-31 | v3   | T-004/T-008 取消整单预算候选过滤，并保留按 Agent 自身报价执行的新入驻风险上限；任务结构与勾选状态不变 |
| 2026-09-04 | v4   | T-004/T-008 改用 3 个真实结算任务解除冷启动风险；评分先验不再参与门禁 |

## 项目信息

- 项目名: ai-agent-collaboration-protocol
- 架构类型: 多服务架构
- specs 路径: specs/7.task-visibility-and-mode/

## 任务列表

### 功能 1: 可见性脱敏

- [x] T-001: 实现 `RedactTaskForAudience()` 权威脱敏函数与私密任务访问权限校验 ~30min
- [x] T-002: 接入市场列表、详情、发布者预览三个接口，统一调用 T-001 ~30min

### 功能 2: 分配模式与验收模式

- [x] T-003: 扩展 `tasks` 表字段（`assignment_mode_config`、`acceptance_mode`、`acceptor_config`）migration ~15min
- [x] T-004: 实现 `ValidateHardConstraints()`（健康状态/准入状态/币种/截止时间/冷启动期单次报价上限；`[v4]` 按真实结算任务数解除，预算偏好和评分样本不参与候选资格）与验收模式前置校验 ~30min
- [x] T-005: 实现 `PATCH /api/tasks/:id/mode-settings`（含接单后锁定、审计日志写入） ~30min

### 功能 3: 前端

- [x] T-006: 实现公开任务市场页（关键词/分类/标签/状态筛选） ~30min
- [x] T-007: 实现「我的任务」工作台页（独立统计口径） ~30min

### 集成与测试

- [x] T-008: 编写测试：私密任务越权、脱敏字段、模式锁定、自动验收前置条件，以及 `[v4]` 0/1/2 个真实结算任务仍受冷启动报价限制、达到 3 个即解除且不受评分样本影响 ~30min

## 依赖关系

- T-002 依赖 T-001
- T-004、T-005 依赖 T-003
- T-006 依赖 T-002；T-007 独立可并行
- T-008 依赖 T-002、T-004、T-005
- 跨 feature 依赖：T-003 依赖 `4.T-001`（`tasks` 表已存在）；`[v4]` T-004 依赖 `3.T-001`（`agent_status_config` 提供报价分位数和真实结算任务阈值）以及任务/分配表中的结算事实，不再依赖 Feature 12 评分快照。

## 风险点

- `RedactTaskForAudience` 一旦被绕过（新接口直接序列化任务实体）将造成信息泄漏，需要在代码评审清单中固化检查项，而非仅依赖测试覆盖。
