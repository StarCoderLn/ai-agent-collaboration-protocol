# 争议与人工仲裁 — 任务清单

## 任务版本

| 日期       | 版本 | 说明     |
| ---------- | ---- | -------- |
| 2026-08-20 | v1   | 初始任务 |
| 2026-08-23 | v2   | 完成资金冻结、角色仲裁、链上执行确认、完整争议审计和前端验收 |

## 项目信息

- 项目名: ai-agent-collaboration-protocol
- 架构类型: 多服务架构
- specs 路径: specs/13.dispute-and-arbitration/

## 任务列表

### 功能 1: 数据模型

- [x] T-001: 编写 `disputes`、`dispute_evidence`、`arbitration_decisions` 表 migration ~30min

### 功能 2: 争议创建与证据

- [x] T-002: 实现争议创建 API（时限校验、触发任务状态迁移至「争议中」、资金冻结生效） ~30min
- [x] T-003: 实现证据提交 API（双方、截止时间校验、复用附件规则） ~30min

### 功能 3: 仲裁决定与执行

- [x] T-004: 实现仲裁决定 API（角色权限校验、支付/部分支付/退款、写审计日志） ~30min
- [x] T-005: 实现仲裁执行对接（触发链上 `release`/`refund`，`decided`→`executed` 两阶段状态） ~30min

### 功能 4: 前端

- [x] T-006: 实现争议提交与状态查看页（证据截止时间、最终决定展示） ~30min
- [x] T-007: 实现仲裁员操作页最小版本（决定录入、理由填写） ~30min

### 集成与测试

- [x] T-008: 编写测试：争议中资金冻结生效、非仲裁员调用决定接口被拒绝、链上未确认前不展示已完成、仲裁记录可审计 ~30min

## 依赖关系

- T-002、T-003 依赖 T-001
- T-004 依赖 T-001、T-003
- T-005 依赖 T-004
- T-006 依赖 T-002、T-003；T-007 依赖 T-004
- T-008 依赖 T-002~T-005
- 跨 feature 依赖：T-002 依赖 `4.T-001`（任务状态机支持「争议中」迁移）；T-005 依赖 `6.T-006`（链上结算/退款触发与确认接口）

## 风险点

- Feature 14 已延后；MVP 的仲裁员权限不是前端占位，而是由共享
  `platform_actor_roles(role='arbitrator')` 在服务端统一校验。后续 Feature 14 只复用并扩展
  该角色表，不替换 Feature 13 的权限边界。

## 验收证据（2026-08-23）

- `0012_dispute_arbitration` 在隔离 PostgreSQL 测试库完成 down/up，五张表与默认配置重建成功；
  临时数据库和 schema dump 已删除，共享验收库未回滚。
- PostgreSQL 垂直集成覆盖：双方查询/证据、普通结算 job 取消、非仲裁员拒绝、金额守恒、
  `decided → submitted → executed`、链确认前持续冻结、按 disputeId 查询完整执行审计。
- 资金 worker 集成覆盖签名交易重播、失败重试/死信、争议状态下取消普通验收结算。
- Web 测试覆盖刷新后从历史 SSE 事件恢复 disputeId，以及 submitted 状态明确展示
  “处理中（等待链上确认）”而非“已完成”。
- Business API 全量：117 个套件成功，248 项通过、0 失败、1 项 Anvil 适配器因未提供
  `ANVIL_RPC_URL` 跳过；Web 全量 27 个文件、84 项通过；两端生产构建通过。
