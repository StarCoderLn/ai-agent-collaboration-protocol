# 运营后台与核心指标 — 任务清单

## 任务版本

| 日期       | 版本 | 说明     |
| ---------- | ---- | -------- |
| 2026-08-20 | v1   | 初始任务 |

## 项目信息

- 项目名: ai-agent-collaboration-protocol
- 架构类型: 多服务架构
- specs 路径: specs/14.ops-backend-and-metrics/

## 任务列表

### 功能 1: 权限与二次确认基础设施

- [ ] T-001: 实现 `roles`/`user_roles` 表、`RequirePermission()` 中间件与高风险操作二次确认 token 机制 ~30min

### 功能 2: 运营查询

- [ ] T-002: 实现运营任务查询 API（状态/时间/异常类型筛选） ~30min
- [ ] T-003: 实现派发失败查询与超时处理入口 API ~30min
- [ ] T-004: 实现交易核对展示 API ~15min

### 功能 3: 指标

- [ ] T-005: 实现六项 MVP 成功指标的计算口径与每日聚合任务 ~30min

### 功能 4: 前端

- [ ] T-006: 实现运营管理后台页面（任务查询/派发失败/超时处理/交易核对/Agent 审核入口整合，高风险操作走二次确认组件） ~30min
- [ ] T-007: 实现核心指标看板页面 ~30min

### 集成与测试

- [ ] T-008: 编写测试：非授权角色调用被拒绝、二次确认未完成不执行高风险操作、审计日志可查询、指标计算口径正确 ~30min

## 依赖关系

- T-002、T-003、T-004 依赖 T-001
- T-005 独立可并行（依赖已有事件数据）
- T-006 依赖 T-001、T-002、T-003、T-004
- T-007 依赖 T-005
- T-008 依赖 T-001~T-005
- 跨 feature 依赖：T-001 供 `3.T-004`、`13.T-004`、`15.T-003` 复用；T-003 依赖 `9.T-001`（`dispatch_attempts`）与 `11.T-003`（超时状态）；T-004 依赖 `6.T-004`（`reconciliation_alerts`）

## 风险点

- 本 feature 的 T-001（RBAC 中间件）是 [[3.agent-health-lifecycle]] 和 [[13.dispute-and-arbitration]] 权限校验的共同依赖；若开发顺序不按 PLAN.md 推荐顺序执行，那两个 feature 需要先用临时权限占位实现，待本 feature 交付后替换，避免相互阻塞。
