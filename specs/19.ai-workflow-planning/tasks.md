# AI 工作流规划任务

- [x] `T-001` 统一模型接口与 DeepSeek/OpenAI 配置，保留 DeepSeek 环境变量兼容读取。
- [x] `T-002` 使用 LangGraph 实现生成、确定性审查与最多一次修正，并接入 PostgreSQL checkpoint。
- [x] `T-003` 增加 0055 草案修订/head 迁移、乐观锁和确认事务。
- [x] `T-004` 增加 Hono 草案读取、保存、AI 生成和确认接口，以及 AICP v1 签名客户端。
- [x] `T-005` 增加 React Flow 编辑器，支持节点字段、增删阶段、依赖编辑、保存和确认。
- [x] `T-006` 完成 DeepSeek 公网、浏览器与 PostgreSQL 集成验收。
- [x] `T-007` 规划协议升级为 v2，接入实时 Agent 契约清单并增加确认前执行能力门禁。
- [x] `T-008` 增加 0058 恢复审计迁移和安全恢复事务，覆盖分配/托管拒绝、完整归档与活动投影重建测试。
- [x] `T-009` 已关闭：原计划使用 v2 为任务 `fc5558b6-eb33-464b-bb5b-42defb8e1572` 重跑外部模型规划；现有任务已恢复为 10 个阶段均有候选、总价 15 USDC，且后续正式任务已完成真实模型规划到 Sepolia 结算的完整闭环，因此不再重复调用模型验收。本项关闭不表示曾对该历史任务执行 v2 外部模型重规划。
- [x] `T-010` 增加剩余阶段一键采用首选 Agent，并在单选成功后自动聚焦下一未选阶段。

## 选人体验验收证据（2026-09-17）

- 批量入口位于当前阶段候选选择区，关系图头部只保留流程信息与统计；确认弹窗列出所有未选阶段、服务端首选候选和最终总价。
- 浏览器不提交 Agent 清单或金额；Marketplace API 在同一 PostgreSQL 事务内锁定任务、工作流节点和最新候选快照，保留已有选择，任一候选失效则整体回滚。
- 批量确认只把任务推进到 `awaiting_escrow`，不创建托管意图、不扣款、不派发；相同幂等键不会重复事件或版本。
- 单选只有在服务端返回成功后才自动聚焦下一未选择阶段，失败仍停留在当前候选并显示原错误。
- 服务端单元测试 22/22、工作流选人 PostgreSQL 集成测试 8/8、前端组件测试 35/35 通过；两端 TypeScript 检查通过。

## 执行能力门禁与恢复证据（2026-09-17）

- 根因是旧计划把 `DesignArtifact/DeckArtifact/QaArtifact` 与目录中实际的
  `DesignSpec/PresentationArtifact/ReviewReport` 混用，并把演示文稿阶段误设为 Coding 分类；
  前 6 个阶段有候选、后 4 个阶段没有候选，并非 Embeddings 或模型调用失败。
- v2 规划请求只携带实时可执行的完整契约组合；确认事务再次锁定 active Agent 目录，缺失执行者
  时返回 `WORKFLOW_PLAN_NO_EXECUTABLE_AGENT`，不会创建正式 run/node/edge。
- 体验库已应用 0058。当前错误任务在确认无选人、分配、托管和执行事实后完成归档恢复，archive
  为 `8079457b-a495-4146-820e-84d9ea8421ee`。旧计划被浏览器再次确认后，第二次恢复保存 archive
  `45988513-fbc7-4669-9c22-0000b2e08002`，证明同一任务可重复安全恢复且旧审计不会被覆盖。
- 当前任务的 4 个错误阶段已按实时目录修正为
  `ResearchArtifact → DesignSpec → PresentationArtifact → ReviewReport`，HTML/PPTX 阶段不再误用
  Coding 分类。重新确认后 10 个阶段均有 1 个 `semantic_v1` 候选，页面显示总价参考 15 USDC；
  浏览器刷新及服务重启后复核通过，选人、托管和链上交易仍为 0。
- PostgreSQL 集成测试覆盖分配存在、托管存在时拒绝恢复，以及候选、曝光、V2 影子任务和节点
  事件完整归档后再删除活动投影；测试事务最终回滚，不污染体验数据。
- T-009 原拟把任务标题、描述、验收标准和实时 Agent 能力清单发送给当前配置的外部模型。
  2026-09-20 复核时，浏览器中的该历史任务已显示 10 个阶段均有候选、候选总价 15 USDC；后续任务
  `d8719b00-94e7-460b-984a-b77bfb6fcb9e` 又完成了真实 DeepSeek 规划、7 阶段执行、交付验收与
  Sepolia 原子结算。继续针对该历史任务重跑模型不会覆盖新的产品能力或资金路径，因此按范围决策
  关闭，不再调用外部模型；不得把该关闭记录解释为“已执行 v2 外部模型重规划”。

## 规划边界验收证据（2026-09-16）

- 固定真实需求“调研新能源汽车市场并制作路演材料”由 DeepSeek 生成 7 个合法阶段。
- 浏览器把首节点改为“需求范围确认”，新增第 8 阶段并连接依赖；确认后显示 8 个正式执行阶段。
- 体验库只读核对：head=`confirmed`，1 个 run、8 个 node、8 个业务 edge、0 个候选记录、0 个 escrow intent。
- PostgreSQL 集成测试在事务内覆盖 template/AI/user 三类修订、版本冲突、确认锁定、正式 DAG 原子固化和无候选/托管副作用，结束后回滚。
- 这一阶段只验证“确认前不产生交易事实”：未广播 Sepolia 交易，也未修改既有任务、钱包或链上事实。

## 正式执行闭环补充（2026-09-16）

- 任务 `d8719b00-94e7-460b-984a-b77bfb6fcb9e`、run `5181698b-94ee-4e22-a797-414625520239`
  从确认后的 DAG 完成 7 个真实阶段；未执行的空占位节点及其边、事件和历史匹配记录已在严格前置条件下删除并保留审计摘要。
- 浏览器研究、确定性汇总、DeepSeek 路演结构、9 页 HTML、136.3 KB 可编辑 PPTX 与本机交付质检均已通过；发布者完成最终验收后只创建一笔原子结算 job。
- 12 USDC 托管最终按清单结算 10.5 USDC 毛额、收取 0.35 USDC 平台费并退回发布者 1.5 USDC；Sepolia 交易
  `0x35b29d7245df8b3070c071da46b32acc4d3c01a83906fe51e0acde087a681b38` 已确认。
- 链上 `WorkflowSettled` 的 manifest、evidence root 与数据库一致；任务为 `settled`、job 为 `executed`、run 记录已释放 10.5 USDC 和发布者退款 1.5 USDC，未解决对账告警为 0。
- Escrow 与工作流 PostgreSQL 集成测试合计 16/16 通过，Marketplace API TypeScript、相关 Biome 检查与 `git diff --check` 通过；测试遗留夹具为 0。
